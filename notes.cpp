// composition is when you add the answers to each function call 
// decomp is how u break them down
// base case is letting know when to let the recursion stop
//ex:
        return num+inclusive(num-1);
        //num -1 is the decomposition
        //num + inclusive is the composition
